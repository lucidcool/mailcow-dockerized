MAILCOW_HOSTNAME=mail.lucid.cool
MAILCOW_TZ=Europe/London

# Disable mailcow’s own ACME because Coolify will handle TLS
SKIP_LETS_ENCRYPT=y

# Optional basics
ADDITIONAL_SAN=          # leave empty or add extra hostnames
HTTP_PORT=80
HTTPS_PORT=443
